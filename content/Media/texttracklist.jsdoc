TextTrackList : EventTarget

Spec:
https://html.spec.whatwg.org/#audiotracklist

----
instance[index : Number] : TextTrack

Returns the TextTrack at the specified **index**.

----
instance.length : Number

The number of TextTracks in the list.

ReadOnly:
true

----
prototype.getTrackById(id : String) : TextTrack

----
event.change : listener(event : Event) : undefined

----
event.addtrack : listener(event : Event) : undefined

----
event.removetrack : listener(event : Event) : undefined
