VideoTrackList : EventTarget

Spec:
https://html.spec.whatwg.org/#videotracklist

----
instance[index : Number] : VideoTrack

----
instance.length : Number

ReadOnly:
true

----
instance.selectedIndex : Number

ReadOnly:
true

----
prototype.getTrackById(id : String) : AudioTrack

----
event.change : listener(event : Event) : undefined

----
event.addtrack : listener(event : Event) : undefined

----
event.removetrack : listener(event : Event) : undefined
